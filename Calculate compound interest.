#include <iostream>
#include<cmath>
using namespace std;
int main() {
    float p,r,t,ci,amount;
    cin>>p>>r>>t;
    amount=p*pow((1+ r/100),t);
    ci=amount-p;
    cout<<"Total Amount= "<<amount<<endl;
    cout<<"Intrest Amount= "<<ci<<endl;
    return 0;
}
